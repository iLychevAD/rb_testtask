### How to run
In the repo root directory do `docker-compose.yml up`

Run `docker exec -it bash` to the `client` container and run check bash scripts inside it.

The bash scripts in the `/check_scripts` demonstrate different scenarious described in the task.

The numeric prefixes of the bash scripts signify the order they should be run in.